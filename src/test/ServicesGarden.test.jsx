import React from "react";
import { shallow } from "@testing-library/react";
import ServicesGarden from "../components/ServicesGarden";

it.skip("renders without crashing", () => {
  shallow(<ServicesGarden />);
});